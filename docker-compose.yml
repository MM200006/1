services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - app-data:/app/data
      - ./credentials:/app/credentials:ro
    restart: unless-stopped

volumes:
  app-data:
